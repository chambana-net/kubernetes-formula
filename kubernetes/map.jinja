{% set kubernetes = salt['grains.filter_by']({
    'Debian': {
        'resource_dir': '/srv/kubernetes',
        'config_dir': '/etc/kubernetes',
        'prefix': '/opt/kubernetes',
    },
}, merge=salt['pillar.get']('kubernetes:lookup')) %}
